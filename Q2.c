include <stdio.h>

int main() {
    int num1, num2, sum;  // three variables declare kori lolu
    // User ot 1st number to input koribo dile
    printf("Enter the first number: ");
    scanf("%d", &num1);

    // User ot 1st number to input koribo dile
    printf("Enter the second number: ");
    scanf("%d", &num2)
    // duta number tu add kori heotor results to sum bule variable ot store kori le
    sum = num1 + num2;

    // result to dekai dile
    printf("The sum of %d and %d is: %d\n", num1, num2, sum);

    return 0; 
}
